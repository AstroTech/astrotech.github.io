language: ruby

rvm:
- 2.0

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of html-proofer

install: bundle install jekyll html-proofer
script: jekyll build && htmlproof .site/


